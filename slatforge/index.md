---
title: SlatForge
layout: default
nav_order: 1
---

# SlatForge Documentation

SlatForge is a Blender addon that connects to a local SlatForge server to generate textured 3D models from single reference images.

Use the pages below to learn how to install the addon, load reference images, run mesh and texture generation jobs, reprocess the latest mesh, and fix common issues.